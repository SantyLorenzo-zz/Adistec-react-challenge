import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
  width: 100%;
  height: 100%;
  background-color: white;
`;

const CreateBundle = () => <Container>CreateBundle</Container>;

export default CreateBundle;
